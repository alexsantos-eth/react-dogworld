/**
 * Local storage data for dogs
 */
interface LocalDog {
	noBread?: boolean // This family does not have a sub bread
	dogName: string // Dog name
	family: string // Dog family name
	like: boolean // Save as favorite
}
